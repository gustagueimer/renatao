#include <stdio.h>
#include <windows.h>
#include <math.h>
#define balls int vMax = 10;

typedef struct cliente
{

};


int main() {
    balls
    int c = 1;
    float balls3[vMax];
    while (c < 11) {
        printf("insira numero - ");
        scanf("%f", &balls3[c]);
        c++;
    }
    for (int i = 1; i <= 10; i++) {
        printf("%i numero - %f\n", i, balls3[i]);
    }
    system("pause");
    return 0;
}